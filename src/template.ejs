<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Tic-Tac-Toe</title>
  </head>
  <body>
    <div id="root"></div>

    <% htmlWebpackPlugin.files.js.forEach((source) => { %>
      <script src="<%= source %>"></script>
    <% }); %>
  </body>
</html>